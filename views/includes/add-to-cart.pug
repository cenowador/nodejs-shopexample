form(action="/cart", method="POST")
    button.btn(type="submit") Add Carrinho
    input(type="hidden", name="productId", value=`${product._id}`)
    input(type="hidden", name="_csrf", value=`${csrfToken}`)